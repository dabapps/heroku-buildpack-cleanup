#!/usr/bin/env bash

BUILD_DIR=$1

dirs=(
    node_modules
    static
)

for dir in "${dirs[@]}"
do
    if [ -d "$BUILD_DIR/$dir" ]; then
        echo "Removing $dir/"
        rm -r "$BUILD_DIR/${dir:?}"
    fi
done
